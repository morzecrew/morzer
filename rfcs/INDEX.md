# RFCs

Design proposals for morzer. Each RFC captures a design before or while it is
built: the problem, the state of the code today, the locked decisions with
their rationale, and what is deliberately excluded.

## Allocating a number

The next free number is **0015**. Before creating an RFC, glance at the table
below (or `ls` this directory) and take the next unused integer — numbers
collide when minted in parallel. Update this table in the same change.

Filename: `NNNN-kebab-title.md`. Keep the `# RFC NNNN — Title` H1 and the
number in the filename in sync.

## Index

| # | Title | Status | One-line |
|---|---|---|---|
| [0001](0001-update-and-rollback.md) | Update and rollback | ✅ Complete | Adds `update` and `rollback` as engine operations: fetch, verify against a recorded digest, gate on `compatibility`, take a pre-update backup, swap the release pointer, and compensate by reverting it. Wires the already-written but uncalled `CheckUpgrade`/`AssessRollback` — P1–P2 shipped: both functions under test (which found two defects), a second bundle fixture, `update` with fault injection at six points, `rollback` with its three-question refusal, and `--to` on both — without which a second rollback returns to where the first started. Never rolls a database back automatically — the assessment reports containers, schema and restore-required separately, and refuses rather than guesses. |
| [0002](0002-rich-terminal-renderer.md) | Rich terminal renderer | ✅ Complete | The Bubble Tea live step list behind `ModeRich`, plus styled plan, doctor and status views and `status --watch`. The renderer is a bus subscriber with no back-channel: it cannot influence control flow, a panic in it is contained, and plain mode stays the reference — enforced by a parity test that has already found a gap in plain rather than in rich. `glamour` release notes remain gated on a bundle shipping a `RELEASE.md`. |
| [0003](0003-secrets-recovery-and-onboarding.md) | Secrets, recovery and onboarding | ✅ Complete | `installation export` / `import` make the offline recovery key usable: a machine whose root is deleted entirely is rebuilt from an export plus the key, keeping its original installation id so its own backups stay restorable — proven against real age keys on every CI run, and it found two defects doing it (§12). `secret edit` bounds the one place plaintext touches a filesystem to a tmpfs directory that is overwritten however the editor exits. `doctor` reports overdue rotations and a render directory that is not tmpfs, both as warnings. `init` asks at a terminal and prints the equivalent command line, so it stays scriptable. |
| [0004](0004-distribution-and-verification.md) | Distribution and verification | ✅ Complete | Four transports behind one scheme-dispatching registry — directory, `tar.zst`, HTTPS and OCI — all producing the same content digest, so pinning a release does not pin a transport. Archive extraction refuses traversal, links, device nodes and bombs, each with a fixture built in the test that refuses it. minisign verification makes `require_signature` a working control. Adds a generated JSON Schema, an offline install path with a `doctor` check for it, and signed reproducible builds. Found that the OCI client does not verify blob contents; both of the RFC's dependency-weight predictions were wrong and are corrected in §9. |
| [0005](0005-continuous-integration-and-release.md) | Continuous integration and release automation | ✅ Complete | A change-gated `quality → test → docs → acceptance → coverage` topology invoking `just` recipes, so local and CI cannot drift. Fails the build when a real-adapter contract suite *skips*, which once let CI pass without ever exercising the production secret store. Tagged releases re-run CI as a reusable workflow and refuse a tag that is not on main. The acceptance job runs the whole lifecycle against real Docker in ~40s — and found on its first run that the manifest's digest-pinned images decided nothing at all. |
| [0006](0006-documentation-site.md) | Documentation site | ✅ Complete | A versioned zensical site under `pages/`: 23 pages across get-started, operating, authoring, reference and explanation, split by audience because operators and bundle vendors were interleaved in a 290-line README that is now 76. Vendor examples are extracted from the test-exercised `testdata/bundle/`. `just docs-check` fails the build on a broken link, a page missing from the nav, or an undocumented command, flag, error code, exit code, manifest field or hook variable — all read out of the source. Diagrams are mermaid rather than d2, which removed a CI binary and a build step. The published site root 404'd until a release existed to redirect to; `dev` now bootstraps it and steps aside at the first tag (§12). |
| [0007](0007-operator-parameters.md) | Operator parameters | ✅ Complete | A release declares the knobs an operator may set — ports above all — typed, validated, and delivered to Compose, templates and hooks as `<PRODUCT>_PARAM_<NAME>`, with `morzer config list/get/set/unset` as a journaled operation that *re-creates* rather than restarts: a restart leaves a changed port in place, proved by an acceptance stage that fails when reverted. `requirements.ports` and health URLs follow the value, closing the defect that made changing a port produce a working deployment and a converge that failed waiting for health. The runtime no longer inherits the invoking shell's environment. Adds a three-tier example the acceptance run installs, reconfigures and probes, and documents and drift-gates all three bundle-facing ABIs — hook environment, Compose interpolation, template render context — removing the render context's `.Env` back door. Written after finding an operator could change *nothing* post-`init`: `installation.yaml` claimed edits took effect and was never read back, and `Installation.Settings` reached templates with no writer. Two limits recorded in §17. |
| [0008](0008-test-coverage-program.md) | Testing the claims: a coverage programme to 95% | ✅ Complete | Raises coverage from a measured 70% to 95%, and makes every advertised security property name the test that enforces it. Written after measuring that `go test` alone reports 59.5% while the union with a `-cover`-instrumented acceptance run is 70% — ten points of real testing were invisible, so every judgement about where to test next was made against the wrong map. Of the remaining gap, 29% is I/O error branches and 71% is reachable behaviour, 604 statements of it whole CLI commands nothing drives. The motivating find: `redactAttr`, the enforcement point for "secrets never reach a log", is 21% covered and its `WithAttrs` path 0% — and redacts eagerly, so a value captured before a secret is registered is written in the clear (demonstrated; not currently reachable, because the only two call sites happen to pass operation ids). Five phases: count what is already tested, drive every CLI command, inject I/O failures, run adapters against real services, and publish a claims-to-tests inventory that is gated like the rest. Shipped 2026-08-04: all five phases complete as designed, union coverage 70.0% -> **81.6%**, an inventory of 50 claims each naming the test that fails without it, and the predicted defect found — the redaction handler claimed to scrub anything that stringifies while handling only `string` and `error`, so a `Stringer` or a struct printed its secret in full. The container suites answer what a fake cannot: `down` really preserves its volume, a `pg_dump` the manager took really restores, and `pg_isready` without `-h` reports the entrypoint's throwaway server as ready. **95% is not met** — §16 names the 1438 statements that stand between here and it, and §17 plans P6–P11 against them. The gap is breadth, not depth: 1438 statements in 1225 blocks across 468 functions, only 55 of them in functions with twenty or more uncovered, so there is no large win left. Two new mechanisms carry it — huh's accessible mode to make the `init` wizard answerable without a pseudo-terminal, and a corrupt-archive builder for extraction's refusals — and everything else is fixtures against switches that already exist. P6–P11 then shipped: the `init` wizard is answerable without a pseudo-terminal through huh's accessible renderer, sops failures and hostile archives are driven one refusal at a time, and the operation steps are failed at the supervisor, the renderer and a read-only filesystem. **86.1%, and the target is not met** — 699 statements short, because the §17 estimates were roughly three times optimistic about statements that cluster behind one hard prerequisite. Two more claims-without-enforcement found on the way: `preflight.NoUnfinishedOperation` is documented and wired into nothing, and the scripted runner this programme added returned no `*ExitError`, so an adapter classifying a failure saw a success. Closed 2026-08-04 at 86.1% with the target revised rather than met (decision 13): the claims inventory is the deliverable and is finished, the percentage was always the proxy, and what remains is named in §19 rather than promised. |
| [0009](0009-backup-targets.md) | Backup targets: getting a backup off the machine | ✅ Complete | Adds a `BackupTarget` port and a scheme registry — `file://`, `ssh://`, `s3://` — so a backup lands somewhere the machine that took it cannot delete. Mirrors the release-source registry byte for byte rather than inventing a second shape. Every backup today is on the disk that will fail: the recovery documentation already tells operators to copy them off by hand, and the recovery test simulates it with a `copyTree` that this RFC lets it stop faking. Pushing happens after verification and before retention, and **a failed push fails the backup** — reporting success for data that is still only on the doomed machine is the thing this exists to prevent. The interesting problem is credentials: restoring from a bucket needs credentials that live in the state that is in the backup that is in the bucket, so the export carries them, flags override, and `file://` needs none. No dedup, no incremental, no restic — §8 records the measurement that would reopen that. Shipped 2026-08-04; §12 records ten dated amendments — a push-failure policy that would have deleted the very backup it was protecting, a host-key pin that failed against honest servers, a listing prefix that deleted a neighbouring backup, a pre-update backup that never left the machine, and six more found by auditing the shipped implementation, including a `--dry-run` that mutated and a second ssh target whose host key was never checked. |
| [0010](0010-compose-volume-capture.md) | Capturing Compose volumes | ✅ Complete | Lets a backup include the project's named volumes, so a release with no backup hook still produces something restorable when there is product data to capture -- bind mounts and excluded volumes do not count, and a release with neither a hook nor a capturable volume is refused rather than given an empty backup -- and a release with a database hook does not silently lose its uploads. The whole RFC hangs on one question: copying a live volume gives a *crash-consistent* copy, not an application-consistent one, so a volume copy of a running database is not a database backup and the design must not let anyone believe it is. **Decision 10 resolved in favour of cold-by-default**: the vendor writes `consistency: hot` where it is true or `exclude` where the hook owns the data, and an undeclared volume is captured with its services stopped — refined so only the services mounting that volume stop, all of them once, and `--no-downtime` *skips and reports* rather than downgrading to a hot copy. Shipped 2026-08-05 with all three phases: the manager's first image of its own (busybox, pinned, local-first, reported by `doctor`), a restore that replaces rather than merges and is refused by name while a mounting service runs, bind mounts reported and never captured, and a space check that refuses before anything is written or stopped. §12 records eleven ways the implementation changed the design — a staging bind mount that would have left root-owned plaintext uploads beside the encrypted backup, a space check that overflowed `int64` and turned its own refusal into a pass, and an error wrap that dropped the `docker pull` hint on the one machine where it mattered. |
| [0011](0011-bundled-container-images.md) | Bundled container images | 📝 Draft | Lets a release carry its own container images so installing it needs no credentials for the vendor's registry — the problem being credential distribution, not network isolation: ECR, Artifact Registry and ACR grant at IAM or project scope, so the smallest credential a vendor can issue is often far larger than intended. Per-image, so a release bundles its two private images and keeps pulling `postgres`. The converge path already accommodates this — `Pull` skips images that are present and `apply --startup` skips it entirely — so what is missing is only the getting-them-here part, and `doctor` already ships `runtime.images-local` warning about a condition with no supported remedy. **The obvious implementation does not work**: `docker save` writes `RepoTags: null` and no `RepoDigests`, so a loaded image never answers to the `name@sha256:…` the manifest pins and `HasImage` inspects — verified, and it is why the design is an OCI layout ingested through an ephemeral loopback registry (the acceptance harness has run one since 0005 for exactly this reason) with a digest-verified local index as fallback. Forging the daemon's own distribution metadata is recorded as rejected: it asserts provenance without checking it, which is the property 0005 found absent once already. Reverses 0004's "pulling container images … unchanged" non-goal and narrows its "the manager does not run a registry". **Design locked 2026-08-08**: a distribution mirror was weighed and rejected — practically available, and it still requires the customer to reach a registry, which for some of them is not true. The extraction ceiling rises via a budget declared in the manifest and read from the tar stream plus a free-space preflight, because extraction precedes verification and the signature cannot back that guard up. |
| [0012](0012-packing-images-into-a-bundle.md) | Packing images into a bundle | 📝 Draft | `morzer release pack` produces the bundles 0011 defines: copies every image the manifest marks `from: bundle` out of the vendor's registry into an OCI layout via `oras-go` — already a direct dependency, so no new supply-chain surface — and regenerates `SHA256SUMS` over the tree. Exists because the manual path scales badly in a specific way: the verifier fails closed on any unlisted file, an OCI layout is hundreds of content-addressed blobs, and `publishing.md` already prescribes a `find \| wc -l` cross-check as the workaround. Refuses fail-closed on a digest that does not match what the manifest pins (the check that makes provenance mean anything), on a stale `SHA256SUMS.minisig`, and on layout entries no manifest names. Signing, archiving and publishing stay out — signing keys are the boundary the manager does not cross — and the Non-goals exist because `pack` makes the manager *produce* an artefact for the first time, which invites "and sign them, and upload them". Gated on 0011 P1, and **design locked 2026-08-08** alongside it: writes in place, verifies the finished bundle by reusing `release verify` so a mismatch is caught on the vendor's machine rather than the customer's, and takes one `--platform` with per-image selection deferred. |
| [0013](0013-bundle-authoring-experience.md) | Bundle authoring experience | 📝 Draft | `release verify` — whose own help calls it "the command a bundle vendor runs in their own CI" — prints `bundle is valid` for a bundle whose template cannot render, demonstrated with an unterminated action and a reference to a field the render context does not have. It validates the manifest, checks referenced files *exist*, and never parses a template, so with `missingkey=error` doing its job the failure lands during an operator's `apply` instead. Makes parsing unconditional and puts rendering against a synthetic context behind `--render-check`, opt-in precisely because invented values cannot promise what a real install delivers — making that the default would turn "verify passed" into a guarantee it cannot keep, which is the failure being fixed. Three ergonomics changes ride along, all convention rather than format because every path is already vendor-named: a `yaml-language-server` modeline pointing at the JSON Schema 0004 already generates and publishes and that nothing points at, so editors give completion and would have caught the unquoted-`mode` trap; `.yaml.tmpl` on templates so an editor stops linting Go template syntax as YAML; and the secret schema out of `templates/`, where it never was one. Amended to carry `release new`, because §5.2–5.4 are retrofits for existing bundles and *defaults* for new ones: without a scaffold they stay documentation, and every bundle authored in the meantime needs the same retrofit by hand. The scaffold's output must pass `verify --render-check` unedited, which couples the two halves — neither the scaffold nor the verifier can move without the other. |
| [0014](0014-building-a-release-bundle.md) | Building a release bundle | 📝 Draft | `morzer release build` and `release archive` — the two commands between a bundle source tree and something publishable, and two rather than one because the signature lives *inside* the archive, so signing has to sit between them and signing is the boundary the manager does not cross. Exists because archive entry order stopped being a convenience the day [0011](0011-bundled-container-images.md) decision 11 locked: the extraction budget is read from the tar stream, so `manifest.yaml` must arrive first — while `tar --zstd -C bundle .` emits directory order and [0012](0012-packing-images-into-a-bundle.md) §8 excluded archiving, leaving a locked decision resting on a step nothing owned. Locks the order as a property of the format, makes 0011's read **fail closed** when it is not honoured, and takes the reproducibility requirement 0012 deferred to whoever owned that step. Also stamps the version, optionally from `git describe`, as `<next-patch>-dev.<distance>.g<sha>` — and three of that scheme's choices are forced by measurements against the pinned semver library rather than taste: a prerelease sorts *below* its own release (so the patch must be bumped, which is `setuptools-scm`'s `guess-next-dev` arrived at from the same constraint), build metadata is retained by `String()` yet ignored by `Compare()` (so metadata-differing builds land in different release directories that the never-republish check never compares — a silent bypass of the guard, closed by refusing `+metadata` in `metadata.version`), and OCI tag grammar excludes `+` (so the sha is a prerelease identifier). Refuses a dirty tree, and fails loudly rather than defaulting when no tag is reachable — the shallow-clone failure `actions/checkout` produces by default. |

## Status legend

- 📝 **Draft** — proposed, not started
- 🚧 **In progress** — partially shipped
- ✅ **Complete** — fully shipped
- ❌ **Rejected / withdrawn**
