# demo 1.2.0

The release notes a bundle ships, declared as `metadata.release_notes` so the
manager finds them by declaration rather than by convention.

## Changed

- Nothing. This is the fixture the test suite installs, updates, backs up and
  rolls back on every run.
